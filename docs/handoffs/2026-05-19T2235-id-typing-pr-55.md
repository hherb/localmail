# NEXT_SESSION.md — localmail handoff

> **Status as of 2026-05-19 (end of session).** PR **#53**
> (force-download + MIME clamp, closes #32 phase 1) merged at the start
> of this session as squash commit `0b310c3`. Follow-up issue **#54**
> (Range support, phase 2 of #32) was filed before this session.
>
> This session shipped PR **#55** (`feat(api): unify ID typing on the
> wire — strings in/out (closes #33)`) — branch
> `feat/unify-id-typing-wire-strings`, single commit `7e2aa84`,
> **550 tests pass** (535 baseline + 15 new), mypy clean on touched
> files. PR #55 **OPEN** at session close.

## Project context (1-minute version)

`localmail` mirrors IMAP accounts (Gmail OAuth, password) into Postgres.
**Strictly read-only with respect to IMAP**. Downstream consumers read
DB + attachment tree directly or via the `localmail serve` HTTPS API.
See [CLAUDE.md](CLAUDE.md), [README.md](README.md), and
[docs/superpowers/specs/2026-05-17-localmail-gui-design.md](docs/superpowers/specs/2026-05-17-localmail-gui-design.md).

## What this session shipped

| SHA | What |
|---|---|
| `7e2aa84` | `feat(api): unify ID typing on the wire — strings in/out, ints in DB (closes #33)` — new `localmail.api.ids.parse_int_id` boundary helper; `account_id` / `message_id` / `since` are now `str` on the wire; non-digit input → `400 /problems/validation-failed`; `changes.py` refactored to share the helper; GUI design spec + CLAUDE.md document the invariant. |

PR **#55** opened against `main` (status: **OPEN** at session close).

### Issue closed (by PR #55)

- **#33** — `api: unify ID typing across endpoints (strings on the wire,
  ints in DB)`. Breaking API change, but the in-tree Tauri client (the
  only external consumer today) already uses string IDs, so no
  downstream work was needed. The Tauri client was verified before
  shipping (`gui/src-tauri/src/commands/*.rs`, `gui/src/lib/**/*.ts` —
  every `account_id` / `message_id` is `String` / `string`).

### Concrete deliverables in PR #55

- New module
  [`src/localmail/api/ids.py`](src/localmail/api/ids.py) —
  `parse_int_id(value: str, *, field: str) -> int`. Strict: rejects
  empty, leading `+`/`-`, surrounding whitespace, decimals, hex
  prefixes, and Unicode digit characters (`"²"`, `"٢"`) that `int()`
  happens to accept. The implementation is two short lines:
  `if not value or not value.isascii() or not value.isdigit(): raise
  ValidationFailed(...); return int(value)`. Module docstring
  documents the boundary-cast contract for transports (HTTP, MCP).
- [`src/localmail/serve/routes/accounts.py`](src/localmail/serve/routes/accounts.py)
  — `account_id: int` → `account_id: str`; `parse_int_id(...)` at the
  top of `get_folders`.
- [`src/localmail/serve/routes/messages.py`](src/localmail/serve/routes/messages.py)
  — same for `detail` and `raw` on `message_id`.
- [`src/localmail/serve/routes/changes.py`](src/localmail/serve/routes/changes.py)
  — refactored the inline `try/except int()` for the `since` cursor
  into a single `parse_int_id(since, field="since cursor")` call.
  `ValidationFailed` import removed (no longer used directly).
- [`tests/test_api_ids.py`](tests/test_api_ids.py) — 11 unit tests
  covering positive digits, zero, BIGSERIAL > 2^53, negatives, `+`
  prefix, alpha, empty, decimals, surrounding whitespace, hex
  prefixes, and error-message-shape assertions.
- [`tests/test_serve_accounts_routes.py`](tests/test_serve_accounts_routes.py)
  — +1 test: malformed `account_id` → 400 problem+json with
  `/problems/validation-failed` and `"account_id"` in detail.
- [`tests/test_serve_messages_routes.py`](tests/test_serve_messages_routes.py)
  — +3 tests: malformed `message_id` on `/v1/messages/{id}`,
  malformed on `/v1/messages/{id}/raw`, and a positive test
  confirming digit-string path params still resolve to the row.
- [`docs/superpowers/specs/2026-05-17-localmail-gui-design.md`](docs/superpowers/specs/2026-05-17-localmail-gui-design.md)
  — "IDs" bullet upgraded from "serialized as strings in JSON" to
  the full boundary contract (strings in *and* out, validated by
  `parse_int_id`).
- [`CLAUDE.md`](CLAUDE.md) — new invariant under "ID typing (#33)":
  declare path/query params as `str`, call `parse_int_id`, never
  bypass the helper.

Test surface: 535 → 550 (+15 new tests). No baseline mypy/test
regressions introduced.

## What's next — concrete acceptance criteria

PR #55 needs to merge first. Once it does:

### 1. Merge PR #55 and clean up

```bash
gh pr view 55                       # confirm CI green
gh pr merge 55 --squash --delete-branch
git checkout main && git pull
```

### 2. Pick the next open issue

Top candidates (handoff-prioritised; #33 / #37 / #9 / #32-phase1 closed):

- **#54** **Range support — phase 2 of #32.** Add `Accept-Ranges:
  bytes` + `Range: bytes=…` parsing + `206 Partial Content` /
  `416 Range Not Satisfiable`. Useful for large PDFs / videos and
  for connection-drop resume on the GUI side. Acceptance criteria
  already enumerated in the issue body. Constraint: keep using
  `open_attachment_bytes` (ACL must remain in effect); `.seek(start)`
  then bounded `read(end - start + 1)` — don't slurp the whole blob
  into memory.
- **#38** `/v1/changes` semantics — pick one of: keep
  tail-subscription + point clients at `/v1/messages` for backfill;
  add `min_id`; add a separate paginated `/v1/messages?since=&before=`
  endpoint. Best decided after observing what the GUI actually does
  in production. May still be premature.
- **#5** Batch INSERT for chunking loop. Perf follow-up; useful once
  archives are large. Touches `embed_worker.py`. Defer until someone
  measures backfill on a 100k+ archive.
- **#47** Third-party transient classes (deferred follow-up to #36).
  Still gated on having real operational data showing which classes
  fire. Resist pre-emptive widening.
- **#25** `websockets.legacy` deprecation. **Not actionable** — needs
  upstream uvicorn release with the new `websockets.asyncio` API.
  Re-check on next uvicorn bump.

### Other open issues (unchanged)

- **#28 / #27 / #24 / #22 / #18 / #17** GUI client polish & CI.
- **#10 / #12** Persist Content-ID on attachments (inline `cid:`
  rendering).
- **#7** IP-based / global login rate limiter.
- **#4** Embedding backend — explicit model paths + verified
  query/document task prefixes.
- **#2** Migration 0006 — make GIN index builds CONCURRENT for
  production upgrades.

## Open decisions & risks

1. **`parse_int_id` is strict — leading zeros are accepted but
   `"01"` resolves to `1`.** Two stable encodings of the same int
   are therefore both valid path params. This is harmless (SQL gets
   the same int), but if a future caller treats path params as
   opaque strings and compares them, they could double-count
   resources. Document the canonical form in the OpenAPI spec when
   we add one.

2. **Tauri client wasn't changed even though the contract is now
   stricter.** Tauri already builds URLs from string IDs and the
   server still accepts well-formed digit strings unchanged. Watch
   for any path where the Tauri client constructs an ID from
   anywhere other than a server response — a stale int conversion
   could leak through. None spotted in the audit (`grep` over
   `gui/`), but flag any new code that does `id.toString()` from
   numeric local state.

3. **`/v1/changes` `since` cursor now uses the same helper as
   path params.** Previously the cursor accepted negative ints (via
   bare `int()`); now it rejects them. No known client sends
   negatives (the cursor is mint by the server as `max_id`), so
   this is a tightening that's invisible in practice. If a
   downstream consumer ever sends a synthesized non-positive
   cursor, they'll get a 400.

4. **MIME clamp list (from PR #53) is small on purpose.** Only
   actively script-executable MIMEs are clamped to octet-stream.
   PDFs, images, audio, video, and `text/plain` are served with
   their stored MIME unchanged — Content-Disposition: attachment
   is the actual XSS fix; the clamp is defense-in-depth. Carries
   forward unchanged.

5. **No range support yet (filed as #54).** Big PDFs / videos can't
   resume on a dropped connection and the GUI can't seek into
   media. `Accept-Ranges: none` is honest, but it's a Phase 2
   follow-up.

6. **`rrf_k=60` is the centre of a flat plateau, not an empirically
   verified optimum** (#35 outcome). Unchanged from prior handoff.
   No action needed until a new corpus measures sensitivity.

7. **Dependabot — 12 vulnerabilities** (1 high / 9 mod / 2 low) on
   `main` still need triage. Independent of this PR. Run
   `gh api repos/hherb/localmail/vulnerability-alerts` after merge
   to confirm the count hasn't drifted.

8. **`websockets.legacy` DeprecationWarning** (#25) still fires
   during `test_e2e_serve.py`. Pre-existing; upstream uvicorn
   blocker. No action this session.

9. **Pre-#41 single-operator upgrade.** Anyone running a pre-`4e2e2f1`
   build with a created API user will see empty `/v1/accounts`
   until they run `localmail grant-account USERNAME <each-account>`.
   README upgrade note in place; flag loudly before any release.

## Exact commands to resume

```bash
# Where you are.
cd /Users/hherb/src/localmail
git status                                 # feat/unify-id-typing-wire-strings if PR #55 not yet merged

# Quick sanity check.
unset VIRTUAL_ENV && uv sync
unset VIRTUAL_ENV && uv run pytest -q       # expect 550 passed

# Merge PR #55.
gh pr view 55                               # confirm CI green
gh pr merge 55 --squash --delete-branch
git checkout main && git pull

# Triage the next issue.
gh issue list --state open --limit 40
```

## Known gotchas (still load-bearing)

- **`unset VIRTUAL_ENV && uv run …`** — shells frequently have a stale
  `VIRTUAL_ENV` pointing at some other pyenv venv; `uv run --active`
  picks the wrong interpreter without this.
- **`LOCALMAIL_TEST_DSN` defaults to `postgresql:///localmail_test`** —
  tests must not touch the live `localmail` DB.
- **Migrations 0011–0017 are additive.** Re-running `init-db` on an
  older archive is safe (idempotent), but back up first if the archive
  is non-trivial.
- **`docling` is the Phase 2 extractor.** Install with
  `uv sync --extra extraction` if you need attachment-text extraction
  locally.
- **TLS for `localmail serve`** — `--bind 0.0.0.0` requires `--tls-cert`
  + `--tls-key`. `--no-tls` is only honoured on `127.0.0.1`. Use
  `localmail rotate-tls` to generate a self-signed cert.
- **First-time `body_lang` install** — run `localmail lang-backfill`
  (or `embed-backfill`, which drains both queues) once after upgrade so
  `lang:` queries return rows.
- **ACL upgrade**: post-0016, new API users have **no grants**. Run
  `localmail grant-account USERNAME ACCOUNT_NAME` once per pair.
- **nh3 style-attribute whitespace**: nh3 emits compact
  `color:red` (no space after `:`); never assert exact whitespace in
  sanitiser tests — assert that the property and value both survived.
- **nh3 `attribute_filter` ordering**: the callback runs *after* the
  tag/attribute allowlist check but *before* the URL-scheme check.
  Schemes that aren't in `url_schemes` are stripped before the filter
  sees them, so anything the filter must reach must be in
  `_ALLOWED_URL_SCHEMES`.
- **extract_worker transient classification**: an exception is
  transient iff `isinstance(e, TransientExtractorError)` OR an instance
  of `(ConnectionError, TimeoutError, MemoryError)` appears anywhere in
  its `__cause__` / `__context__` chain. Third-party HTTP/IO classes
  (e.g. `requests.exceptions.ConnectionError`) are NOT in the set —
  extractors must raise `TransientExtractorError` explicitly to opt
  them in. Add to `_TRANSIENT_EXC_TYPES` only after seeing the class in
  real operations (avoid pre-emptive widening).
- **`<a href>` deny schemes** (#48): `_HREF_DENY_SCHEMES = ("cid:",
  "data:")`. Both schemes must remain in `_ALLOWED_URL_SCHEMES` so they
  reach the `attribute_filter` for img/src handling (cid → attachment
  URL rewrite; data:image/... validated by `_DATA_IMAGE_RE`). Any new
  URL scheme added to `_ALLOWED_URL_SCHEMES` that a browser would
  dereference on `<a href>` MUST be considered for parallel addition
  to `_HREF_DENY_SCHEMES`. The deny prefix match strips leading C0
  controls + ASCII whitespace first (mirrors WHATWG URL parser; see
  `_LEADING_URL_TRIM_RE`).
- **RRF fusion is robust but un-tuned against production data** (#35
  outcome): synthetic corpora are insensitive to `rrf_k` because one
  arm dominates rank-1. Don't conclude that the constant doesn't
  matter — only that the synthetic harness can't measure it. Use
  `tests/acceptance/run_rrf_k_sweep.py --corpus {multilingual,
  attachment}` to re-measure against any new corpus before tuning.
- **`body_lang` worker index** (#40): the lang-detect claim query
  needs `messages_body_lang_pending_idx` to avoid seq-scan as archives
  grow. The schema test in `tests/test_search_schema.py` enforces both
  the index's existence and its planner eligibility — if the worker
  query shape ever drifts (new predicate columns, different ORDER BY),
  update both the migration's WHERE clause and the EXPLAIN test
  together.
- **Daemon pool sizing** (#37): all daemon threads share `Daemon.pool`.
  Its `max_size` is auto-computed via `db.compute_daemon_pool_size(...)`
  unless `DaemonConfig.pool_max_size` is set. The chosen value is
  logged at startup. When adding a NEW long-running worker thread to
  the daemon, bump the formula (slot per worker) — not just the
  headroom — so the contract stays exact.
- **Attachment download invariants** (#32 phase 1): `/v1/attachments/
  {sha256}` ALWAYS emits `Content-Disposition: attachment` with both
  `filename=` (ASCII-sanitised) and `filename*=UTF-8''...`
  (percent-encoded original). Stored mime is clamped to
  `application/octet-stream` if it appears in `_INLINE_RISKY_MIMES`.
  `Accept-Ranges: none` is set explicitly. Don't remove any of these
  — the test suite enforces all three. The filename helper picks the
  earliest carrying message by `messages.id`; if you change that
  pick, update
  `test_get_attachment_filename_prefers_first_carrying_message`.
- **ID-typing boundary** (#33, this PR): path and query parameters
  bearing entity IDs are typed `str` on the route handler signature,
  and `localmail.api.ids.parse_int_id(value, field="…")` is the ONLY
  way to cast to int. Helper rejects empty / `+`/`-` / whitespace /
  decimal / hex / Unicode-digit input with `ValidationFailed`. When
  adding a new ID-bearing endpoint or an MCP tool that accepts an
  ID, declare the parameter as `str` and call `parse_int_id`. Never
  accept `int` directly from the wire (FastAPI's auto-coercion
  emits `422` with an inconsistent shape vs the rest of the API).

## File map (post-#55, on branch `feat/unify-id-typing-wire-strings`)

```
src/localmail/
  api/                               # transport-free service library
    accounts.py acl.py attachments.py auth.py errors.py
    ids.py                           # NEW (#33): parse_int_id() boundary cast
    messages.py sanitize.py search.py
  serve/                             # FastAPI wrapper + TLS + middleware
    app.py middleware.py tls.py
    routes/
      accounts.py                    # account_id: str + parse_int_id (#33)
      attachments.py                 # Content-Disposition + MIME clamp (#32)
      auth.py
      changes.py                     # since cursor uses parse_int_id (#33)
      messages.py                    # message_id: str + parse_int_id (#33)
      search.py version.py
  daemon.py                          # single shared self.pool for ALL workers
  db.py                              # compute_daemon_pool_size() + constants
  search/
    arms.py chunking.py embed_worker.py embeddings.py
    extractor.py                     # TransientExtractorError (#36)
    extract_worker.py                # uses pool=, not conn_factory= (#37)
    lang_detect.py page_cache.py query.py reranker.py searcher.py
gui/                                 # Tauri 2 + Svelte 5 client (already string-IDs)
migrations/                          # 0001 … 0017_messages_body_lang_pending_index.sql
tests/
  acceptance/
    run_recall_eval.py               # Phase 1 multilingual gate
    run_attachment_eval.py           # Phase 2 attachment gate
    run_rrf_k_sweep.py               # #35 sweep harness
  test_api_ids.py                    # NEW: 11 parse_int_id unit tests (#33)
  test_serve_accounts_routes.py      # +1 malformed-id test (#33)
  test_serve_messages_routes.py      # +3 malformed-id / string-id tests (#33)
  test_serve_changes_route.py        # existing 400 test still passes (#33)
  test_api_attachments.py            # #32 filename helper tests
  test_serve_attachments_routes.py   # #32 Content-Disposition + MIME tests
  test_daemon_pool.py                # #37 pool-sizing contract
docs/superpowers/                    # specs + plans
docs/handoffs/                       # frozen NEXT_SESSION snapshots
NEXT_SESSION.md                      # this file
```

End of id-typing session. Single commit `7e2aa84` on
`feat/unify-id-typing-wire-strings`; PR #55 open. Merge it, then pick
**#54** (Range support — concrete acceptance criteria already in the
issue body) or **#38** (/v1/changes semantics decision) — both are
well-scoped and standalone.
